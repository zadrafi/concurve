% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/construct_likelihood.R,
%   R/plot.likelihood_function.R
\name{plot.likelihood_function}
\alias{plot.likelihood_function}
\title{Plot likelihood function}
\usage{
\method{plot}{likelihood_function}(x, parameter = NULL,
  type = c("likelihood", "deviance", "both"), n_points = 200,
  interval = NULL, add_ci = TRUE, ci_level = 0.95, add_mle = TRUE,
  relative = TRUE, main = NULL, xlab = NULL, ylab = NULL,
  col = "black", lwd = 2, ...)

\method{plot}{likelihood_function}(x, parameter = NULL,
  type = c("likelihood", "deviance", "both"), n_points = 200,
  interval = NULL, add_ci = TRUE, ci_level = 0.95, add_mle = TRUE,
  relative = TRUE, main = NULL, xlab = NULL, ylab = NULL,
  col = "black", lwd = 2, ...)
}
\arguments{
\item{x}{A \code{likelihood_function} object}

\item{parameter}{Name of the parameter to plot. If \code{NULL}, plots the first parameter
and displays a message.}

\item{type}{Type of plot: \code{"likelihood"} (relative likelihood), \code{"deviance"}
(profile deviance), or \code{"both"} (two-panel plot)}

\item{n_points}{Number of parameter values at which to evaluate the profile likelihood}

\item{interval}{A length-2 numeric vector specifying the range of parameter values
to plot. If \code{NULL}, automatically determined as ±5 standard errors from the MLE.}

\item{add_ci}{Logical; if \code{TRUE}, adds confidence interval bounds to the plot}

\item{ci_level}{Confidence level for the intervals (default: 0.95)}

\item{add_mle}{Logical; if \code{TRUE}, adds a vertical line at the MLE}

\item{relative}{Logical; if \code{TRUE}, plots relative likelihood; if \code{FALSE},
plots absolute likelihood. Only applies to likelihood plots.}

\item{main}{Main title for the plot. If \code{NULL}, automatically generated.}

\item{xlab}{X-axis label. If \code{NULL}, uses the parameter name.}

\item{ylab}{Y-axis label. If \code{NULL}, automatically determined by plot type.}

\item{col}{Color for the main likelihood/deviance curve}

\item{lwd}{Line width for the main curve}

\item{...}{Additional arguments passed to \code{\link{plot}}}
}
\value{
Invisibly returns the profile likelihood data frame
}
\description{
Plot a likelihood function showing relative likelihood or deviance,
with optional confidence intervals and reference lines.
}
\details{
Creates publication-quality plots of likelihood and deviance functions.
Includes optional confidence intervals and reference lines. For two-panel plots,
the layout is automatically managed.
}
\seealso{
\code{\link{ggplot_likelihood}} for ggplot2 version,
\code{\link{plotly_likelihood}} for interactive plots
}
