% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curve_snowflake.R
\name{curve_snowflake_batch}
\alias{curve_snowflake_batch}
\title{Batch Process Multiple Snowflake Queries for Consonance Functions}
\usage{
curve_snowflake_batch(conn, queries, estimate_col = "estimate",
  lower_col = "lower", upper_col = "upper", conf.level = 0.95,
  steps = 1000, cores = getOption("mc.cores", 1L), table = TRUE)
}
\arguments{
\item{conn}{A DBI connection object to Snowflake database.}

\item{queries}{A named list of SQL query strings.}

\item{estimate_col}{Name of the column containing point estimates.}

\item{lower_col}{Name of the column containing lower bounds.}

\item{upper_col}{Name of the column containing upper bounds.}

\item{conf.level}{Confidence level of the input intervals. Default is 0.95.}

\item{steps}{Number of consonance levels to compute. Default is 1000.}

\item{cores}{Number of cores for parallel computation.}

\item{table}{Logical. If TRUE (default), includes summary tables.}
}
\value{
A named list of concurve objects, one for each query.
}
\description{
Executes multiple queries against a Snowflake database and constructs
consonance distributions for each result set.
}
\examples{
\dontrun{
queries <- list(
  "Model A" = "SELECT estimate, lower_ci, upper_ci FROM results WHERE model = 'A'",
  "Model B" = "SELECT estimate, lower_ci, upper_ci FROM results WHERE model = 'B'"
)

results <- curve_snowflake_batch(conn, queries,
  estimate_col = "estimate",
  lower_col = "lower_ci",
  upper_col = "upper_ci"
)

# Plot comparison
plot_compare(results[["Model A"]][[1]], results[["Model B"]][[1]])
}

}
\seealso{
\code{\link[=curve_snowflake]{curve_snowflake()}} for single query processing

\code{\link[=plot_compare]{plot_compare()}} for comparing results
}
